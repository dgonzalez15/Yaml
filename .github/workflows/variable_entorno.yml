name: Ejemplo de Composite Action

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    env:
      MY_ENV: Primera variable
      


    steps:
      - name: Test
        env:
          STEP_MY_ENV: Segunda variable nivel step
        run: echo "Valor de la variable es $MY_ENV"
            echo "Valor de la variable2 es $STEP_MY_ENV"